package com.cn.hnust.es;

import java.util.Map;

import org.springframework.stereotype.Controller;

@Controller
public class ElasticController {
	
	public void get() {
		// Map map = esService.getFromMatrixByNameIDAndType(type, nameID + "");
		// if (map != null) {
		// String hpURL = (String) map.get("hpURL");
		// if (!imgUrl.equals(hpURL)) {
		// map.put("hpURL", imgUrl);
		// esCurdDao.updateMatrix(map, (String) map.get("_id"), (String)
		// map.get("type"), (String) map.get("_index"));
		// }
		// }
	}
}
